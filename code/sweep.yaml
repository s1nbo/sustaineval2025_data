method: bayes
metric:
  name: eval_accuracy
  goal: maximize
parameters:
  learning_rate:
    distribution: uniform
    min: 1e-8
    max: 5e-5
  weight_decay:
    distribution: uniform
    min: 0.0
    max: 0.3
  epochs:
    values: [6, 8, 10, 12]